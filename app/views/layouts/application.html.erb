<!DOCTYPE html>
<html lang="en">
  <head
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "SuperMarket" %></title>

    <%= stylesheet_link_tag    "application" %>
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.0/css/bulma.min.css"> -->
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <div class="sticky">
        <nav class="top-bar" data-topbar role="navigation">
          <div class="left">
            <%= link_to 'Home', root_path, class: 'navbar-item' %>
            <%= link_to 'All Products', products_path, class: 'navbar-item' %>
            <%= link_to 'All Merchants', merchants_path, class: 'navbar-item' %>
            <% if session[:merchant_id] %>
            <%= link_to 'Dashboard', orders_path, class: 'navbar-item' %>
            <% end %>
          </div>
          <div class="right">
            <%= form_tag search_path, class: 'form-inline', method: :get do %>
              <%= text_field_tag :search, params[:search], class: 'search-bar', placeholder: 'Search...'%>
              <%= submit_tag 'Search', class: 'search-bar button'%>
            <% end %>
            <div class="login">
              <% if @logged_merchant %>
              Logged in as: <%= @logged_merchant.username %>
              <%= link_to "Log Out", logout_path, method: :delete %>
              <% else %>
              <%= link_to "Log in with Google", '/auth/google_oauth2' %>
              <% end %>
            </div>
            <% if session[:cart_id] %>
            <%= link_to 'Cart', viewcart_path(session[:cart_id]) %>
            <% end %>
          </div>
        </nav>
      </div>
    </header>


    <aside class="side-nav">
      <h3>Categories</h3>
      <div class="nav-categories">
        <% Category.categories_with_works.each do |category| %>
        <div class="category-name">
          <%= link_to category.name.capitalize, category_path(category.name) %>
        </div>
        <% end %>
      </div>
    </aside>

    <main>
      <section class='flash'>
        <%= flash[:error] if flash.any? %>
        <%= flash[:success] if flash.any? %>
        <%= flash[:failure] if flash.any? %>

      </section>

      <%= yield %>
    </main>

    <footer>
      <p class='copyright'>Copyright, The Three Marketeers, 2018</p>
      <h4 class='slogan'>It's a shop.. it's a store.. It's a SUPERSTORE!</h4>
    </footer>
  </body>
</html>
s
